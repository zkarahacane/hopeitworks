version: "3.8"

services:
  todo-app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - DB_PATH=/app/data/todos.db
    volumes:
      - todo-data:/app/data
    restart: unless-stopped

volumes:
  todo-data:
