# Gitea persistent data (mounted volume content if using bind mounts)
data/

# act_runner persistent state
runner-data/

# Runner registration token written by setup.sh
.runner-token

# Docker Compose env file generated by setup.sh (contains runner token)
.env

# Gitea credentials written by setup.sh (admin password + API token)
.gitea-credentials
